apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nwcom-route
  namespace: default
spec:
  parentRefs:
  - name: weather-gateway
    namespace: envoy-gateway-system
  hostnames:
  - "napervilleweather.com"
  - "www.napervilleweather.com"
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: "/"
    backendRefs:
    - name: nwcom
      port: 80
